import { AppBar, ThemeProvider, Typography } from "@material-ui/core"
import * as React from "react"
import withTheme, { themeOptions } from "../components/theme"

// markup
const IndexPage = () => {
  return (
    <div>
     Change meeeeeeeeee
    </div>

  )
}

export default withTheme(IndexPage)
